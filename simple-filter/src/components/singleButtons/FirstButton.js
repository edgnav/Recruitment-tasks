import './buttons.scss';
const FirstButton = () => {
  const handleChange = () => {};
  return (
    <button onClick={handleChange} class={'first-button'}>
      All
    </button>
  );
};

export default FirstButton;
